# DeskPet 使用说明（小白版）

> 面向没有开发经验的普通用户。按顺序看完就能装好、连上、用起来。安装到排查，每一步都有「现象 → 原因 → 处理」。
> 如有和本应用版本不符的地方，以屏幕上的实际菜单为准。最新版本见 [Releases](https://github.com/Innoksadk223/DeskPet/releases)。

---

## 0. DeskPet 是什么？为什么必须先装 Hermes？

**一句话**：DeskPet 是一只住在你桌面上的小宠物，也是**赫麦斯（Hermes）的语音助手外壳**。

- **Hermes（`hermes-agent`）= 大脑**：负责听懂你的话、联网搜索、操作终端、处理文件、跑浏览器等。
- **DeskPet = 形象 + 声音**：负责把 Hermes 变成看得见、叫得应、会说话的桌宠。

> ⚠️ **没有 Hermes，DeskPet 无法工作。** Hermes 是前置条件，不是可选项。装好 DeskPet 后第一次启动，它会**自动检测本机有没有 Hermes**（见第 4 节），找不到会明确告诉你怎么办——不会让你「装完没反应」。

---

## 1. 系统要求

| 项 | 要求 |
|---|---|
| 系统 | macOS（Apple Silicon 或 Intel 均可） |
| 大脑 | 本机已安装可用的 Hermes（`hermes serve` 能启动；版本较新即可，过旧会提示不兼容） |
| 网络 | 使用云端语音（豆包 / MiMo）时需要联网；纯本地语音可离线 |
| 麦克风 | 需要（唤醒、语音输入用） |


## 2. 安装 DeskPet 并首次启动

1. 在 [Releases](https://github.com/Innoksadk223/DeskPet/releases) 下载最新的 `DeskPet-*.dmg`。
2. 双击打开 dmg，把 **DeskPet.app** 拖进 **Applications**。
3. 打开 Applications，双击 DeskPet 运行。

> **现象：提示「无法打开，因为无法验证开发者」/「已损坏」。**
> **原因**：应用未在 App Store 上架，需要你手动放行一次。
> **处理**：在 Applications 里**右键 → 打开** → 再点一次「打开」即可（只需一次）。

4. 首次运行会弹**麦克风/语音识别权限**——点「允许」。

> **现象：想开权限时给拒了。**
> **处理**：系统设置 → 隐私与安全性 → 麦克风 / 语音识别，把 DeskPet 打开。

5. 桌宠出现在右上角菜单栏（一个小爪子图标 🐾）和桌面上。首次启动会自动检测并连接 Hermes（见第 4 节）。

**双击没反应？** 可能是已有一个 DeskPet 在运行（防双开是正常的）。看右上角菜单栏有没有爪子图标。


## 3. 权限是什么、为什么需要

| 权限 | 用途 | 在哪里开 |
|---|---|---|
| 麦克风 | 唤醒词检测、语音输入 | 系统设置 → 隐私与安全性 → 麦克风 |
| 语音识别 | 把你说的话转成文字 | 系统设置 → 隐私与安全性 → 语音识别 |
| 通知（可选） | 任务完成时的提示气泡 | 系统设置 → 通知 |

不授权麦克风 = 喊不醒、没法语音输入（只能打字）。


## 4. Hermes 自动匹配（重要）⭐

DeskPet 启动时会按优先级自动寻找本机 Hermes：

`你手动保存的路径 → 系统 PATH → 常见用户/包管理器路径 → ~/.hermes 安装目录 → 系统路径`

| 情况 | 你会看到 | 怎么处理 |
|---|---|---|
| **找到并连上** | 桌宠正常说话「已就绪」，设置里显示 Hermes 版本 | 不用做任何事 |
| **没找到 Hermes** | 启动时桌宠气泡+语音提示：「没找到 Hermes，请先安装或选择」 | ① 先安装 Hermes（见下）② 装好后：右键 → 设置 ▸ 系统 ▸ **重新连接助手服务** |
| **Hermes 装了但位置特殊，没被自动发现** | 提示未找到 | 右键 → 设置 ▸ 系统 ▸ **选择 Hermes 可执行文件…** 手动选 `hermes`（不要选项目目录或配置文件；选完会自动重连并记住） |
| **Hermes 版本过旧 / 不支持** | 提示「请升级 hermes-agent」 | 升级本机 Hermes 后重连 |
| **装了好几个 Hermes** | 自动用第一个可用的 + 设置里显示来源 | 想换另一个：设置 ▸ 系统 ▸ 选择 Hermes 可执行文件… |
| **连接出问题** | 提示「连不上助手服务」 | 右键 → 设置 ▸ 系统 ▸ **重新连接助手服务** |

**Hermes 还没装？**
近期请根据 Hermes 官方文档安装 `hermes-agent` 并能运行 `hermes serve` 即可。装好后回到 DeskPet 右键 → 设置 ▸ 系统 ▸ 重新连接。

> 提示：DeskPet 不负责帮你下载/安装 Hermes，也不会上传你的账号数据；它只做「本机适配」。


## 5. 怎么和它说话（四种方式）

| 方式 | 操作 |
|---|---|
| **直接喊** | 说唤醒词（例如「猫猫」，可在设置里改）→ 它竖起耳朵听你说 |
| **打字** | 右键桌宠 →「输入文字…」 |
| **按住说话** | 右键 →「语音输入」（点开始，说完再点结束） |
| **免唤醒** | 右键 →「持续聆听」勾选后不用喊词直接说话；说退出词（如「晚安」）退出 |

相关设置（右键 → **设置 ▸**）：

| 菜单项 | 说明 |
|---|---|
| 设置唤醒词… | 喊什么词唤醒它 |
| 唤醒灵敏度 | 灵敏 0.15 / 标准 0.25 / 迟钝 0.4（越灵敏越容易被背景音误唤醒） |
| 退出词… | 持续聆听时哪些词退出聆听；多个用**逗号**分隔，如「晚安，再见」 |


## 6. 聊天与派发任务

| 你想做什么 | 这样说 |
|---|---|
| 普通聊天 | 「今天天气怎么样」「讲个笑话」 |
| 派任务 | 「执行任务：查一下明天天气」「帮我搜 Python 教程」 |
| 任务中途改需求 | 「跟任务说：改成红色」 |
| 查任务状态 | 「还在工作吗」「任务状态」（本地即时回答，秒回） |
| 重新开始对话 | 「新开对话」 |
| 查聊天记录 | 「聊天记录」 |

**任务是怎么跑的**（简单理解）：
- 普通聊天 → 主人直接回答；复杂干活 → 自动派给「任务小助手」在后台执行。
- 一个任务跑着又来一个 → 会排队，前一个做完后按顺序来（最多排 5 个）。
- 任务完成 → **气泡显示完整结果，语音直接播报精简版**；全文归档进对话，之后可以追问「刚才查的详细说说」。不会再出现「结果半天没消息」的情况。


## 7. 怎么停止（三种入口，语义不同）

| 入口 | 说/点 | 停什么 |
|---|---|---|
| **GUI** | 右键 →「中断任务」 | **主回复 + 任务一起停**（菜单栏爪子图标同样） |
| **语音** | 「中断任务 / 停止任务 / 取消任务 / 停一下」 | 只停**任务**，主回复不受影响 |
| **语音** | 「停止回答」 | 只停**主回复**，任务不受影响 |
| **语音** | 「全部停止」 | 两侧一起停 |

> 不记得谁在跑也没关系：说了它没在跑，它会如实告诉你「当前没有运行的任务」，不会假装成功。


## 8. 声音：识别与播报

**让桌宠「听得见」**（识别）：右键 → 设置 ▸ 听写识别
- **本地识别**：离线、免费
- **豆包识别**：云端更准、带静音跳过省钱
- **MiMo 识别**：说完停 2 秒后整体上传，多等 1-3 秒属正常

**让桌宠「说得出」**（播报）：右键 → 设置 ▸ 语音播报方式
- **Edge 语音**：免费、联网、音色自然（默认）
- **系统语音**：离线、免费（需先在系统设置下载中文声线）
- **豆包语音**：音色多、更自然，需在「豆包语音设置…」填 Key
- **MiMo 语音**：小米，可自定义音色（见下）

**想用自己/捏出来的声音？** 用 **MiMo 语音**（预置音色 / 一句文字设计音色 / 录音克隆你自己的声音）→ 完整教程见 [DeskPet/MiMo音色指南.md](DeskPet/MiMo音色指南.md)。豆包（火山引擎）见 [DeskPet/豆包语音指南.md](DeskPet/豆包语音指南.md)。

**没声音？** 先看自己是不是点了「静音播报」；再换一个播报方式试试。


## 9. 形象与性格

- **形象**：右键 → 设置 ▸ 形象（多个可选：月薪猫、卡皮巴拉、小鲸鱼…；也可自己放 spritesheet 到 `Pets/`）
- **人设**：右键 → 设置 ▸ 人设（不同性格，切换立即生效；要彻底生效可「新开对话」）

> 形象是「皮」，人设是「性格」——换形象不影响性格，反过来也一样。


## 10. 你的数据都存在哪

| 数据 | 位置 | 说明 |
|---|---|---|
| 桌宠日志 | `~/Library/Logs/DeskPet/deskpet.log` | 排查问题给作者时看这个 |
| Hermes 日志 | `~/.hermes/logs/agent.log` | Hermes 侧日志（模型调用/工具） |
| 配置（人设/唤醒词/Key） | `~/Library/Application Support/DeskPet/config/` 或项目 `DeskPet/history/config/` | 只在本机 |
| 会话正文 | `~/.deskpet/hermes/state.db` | DeskPet 专属 profile 的完整聊天记录 |
| 任务工作区 | `~/.deskpet/hermes/workspace/` | 任务默认的文件/终端操作落在这里 |
| 桌宠身份 | `~/.deskpet/hermes/SOUL.md` | 桌宠的稳定身份设定 |

**要记住**：
- **聊天记录**和**工作文件**是分开的——清聊天历史不会删工作区文件，反之亦然。
- **凭证（Key/token）只存在本机**，打包出的安装包已强制剥离，不会跟着软件分发。

> 想要完整文件映射与机制，见 [ARCHITECTURE.md](ARCHITECTURE.md)（面向开发者）。


## 11. 常见故障排查

### 装完桌宠不回答 / 没反应
- **现象**：桌宠在桌面，但叫它没反应，也没有气泡。
- **原因**：多半是 Hermes 没连上（没装 / 没被发现 / serve 没起来）。
- **处理**：右键 → 设置 ▸ 系统 ▸ 看 Hermes 状态；按第 4 节处理。仍不行看日志 `~/Library/Logs/DeskPet/deskpet.log` 与 `~/.hermes/logs/agent.log`。

### 喊唤醒词没反应
- **现象**：说话它不理。
- **原因**：唤醒词关了 / 灵敏度太低 / 麦克风没权限。
- **处理**：① 右键确认「唤醒词」开启；② 灵敏度调到「灵敏」试；③ 检查系统设置麦克风权限。

### 说话识别成乱码 / 听不准
- **原因**：本地识别口音/噪音影响。
- **处理**：设置 ▸ 听写识别 换「豆包识别」更准。

### 播报没声音
- **现象**：有回复但不出声。
- **原因**：静音了 / 当前声线不可用。
- **处理**：检查是否「静音播报」；设置 ▸ 语音播报方式 换一个；配置了 Key 的服务失败会自动降级（气泡有 ⚠️ 提示）。

### 连不上助手服务 / 桌面显示断开
- **现象**：「连不上助手服务」气泡。
- **原因**：serve 未运行 / 端口被占 / Hermes 路径变了。
- **处理**：右键 → 设置 ▸ 系统 ▸ **重新连接助手服务**；如一直失败，按第 4 节重新选择 Hermes 并重试。

### 提示 Hermes 版本不兼容
- **处理**：升级本机 Hermes 后重连（错误提示里会写明要做什么）。

### 端口占用（9119/9120/9121）
- **现象**：serve 起不来。
- **处理**：DeskPet 会自动尝试下一个端口；一般无需手动处理。持续失败看日志确认占用程序。

### 微信操作（规划中）
- **现象**：目前文档/设置里还没有微信绑定入口。
- **原因**：微信接入功能**尚未发布**。
- **处理**：暂时先用桌面语音/右键操作；发布后本节会更新步骤。

### 其他
| 现象 | 处理 |
|---|---|
| 开机不自动出现 | 右键 →「开机自启」勾选 |
| 设置的词/音色重启后丢了 | 配置见第 10 节；别用「恢复默认设置」误清了 |
| 想完全重置 | 右键 → 设置 ▸ 恢复默认设置…（会重置全部设置） |


## 12. 安全边界（请你心里有数）

- **工作区不是系统沙箱**：`~/.deskpet/hermes/workspace` 只是默认工作目录。你明确让它去别的地方操作时，它仍可能访问——这是翻译过一次 Hermes 能力，不是权限沙箱。
- **敏感 / 付费 / 不可逆操作仍要你把关**：涉及发送、付款、删除、外发等动作，桌宠会先问你确认，不会擅自执行。
- **唤醒词检测在本地**（不把唤醒音频上传云端）；音频/语音云服务按你配置的渠道走。
- **不搜集你的聊天记录上传**；数据都在本机（见第 10 节）。


## 13. 更新、卸载与清理

**更新**：下载最新 dmg，拖进 Applications 覆盖即可（设置与数据会保留）。

**卸载**：
1. 退出 DeskPet（右键 → 退出桌宠）。
2. 把 Applications 里的 DeskPet.app 移到废纸篓。
3. **保留数据**：不动 `~/Library/Application Support/DeskPet/` 与 `~/.deskpet/` 即可日后恢复。
4. **彻底清理**（连数据一起删）：
   - `~/Library/Application Support/DeskPet/`
   - `~/Library/Logs/DeskPet/`
   - `~/.deskpet/hermes/`（会话、工作区、SOUL）
   - 如用源码版：项目目录下的 `DeskPet/history/`

> 版本说明：本指南对应 **DeskPet 0.7.6**。「Hermes 自动适配（第 4 节）」为该版本新增特性。
