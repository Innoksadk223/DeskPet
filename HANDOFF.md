# DeskPet 下一版本更新计划与交接

> 更新时间：2026-08-20
> 当前产品版本：**0.7.0**
> 下一版本：**暂定 0.8.0，版本号待用户最终确认**
>
> 新对话接手顺序：先读 `ARCHITECTURE.md`，再读本文件。本文记录当前已交付状态、下一版本目标、依赖关系和不可直接假设的决策。

---

## 1. 当前已交付基线

### 1.1 GitHub 与安装包

- 仓库：`Innoksadk223/DeskPet`
- 当前分支：`release/0.7.0`
- 当前提交：`6b46655 release: prepare DeskPet 0.7.0`
- 远程分支：`origin/release/0.7.0`
- GitHub Release：<https://github.com/Innoksadk223/DeskPet/releases/tag/v0.7.0>
- DMG：`DeskPet-0.7.0.dmg`
- DMG 直链：<https://github.com/Innoksadk223/DeskPet/releases/download/v0.7.0/DeskPet-0.7.0.dmg>
- Bundle 版本：`0.7.0`，Build `42`
- 架构：Apple Silicon `arm64`
- 打包时已检查 bundle 内 `duoyunApiKey`、`asrApiKey`、`mimoApiKey` 均为空

### 1.2 0.7 已实现的中断语义

- 右键菜单「中断任务」：同时中断主 Agent 和任务 Agent。
- 菜单栏同名入口：同时中断主 Agent 和任务 Agent。
- 主 Agent 或任务 Agent 任一忙时，GUI 中断入口可用。
- GUI 反馈分别说明主侧与任务侧是否停止成功。
- 语音「中断任务」仍是 task-only，只停止任务 Agent。
- 语音「停止回答」仍是 main-only，只停止主 Agent 当前回答。
- 语音「全部停止」是主 Agent + 任务 Agent 双侧停止。

相关代码：

- `DeskPet/Sources/DeskPet/AppDelegate.swift`
- `DeskPet/Sources/DeskPet/HermesBridge.swift`
- `DeskPet/Sources/DeskPet/PetView.swift`
- `DeskPet/Sources/DeskPet/StatusItemController.swift`

### 1.3 当前 Hermes 自动匹配能力（已有，不等于本计划完成）

当前实现已经能在**本机当前用户**环境中探测 Hermes 可执行文件，候选包括：

- `~/.hermes/hermes-agent/venv/bin/hermes`
- `~/.local/bin/hermes`
- `/usr/local/bin/hermes`
- `/opt/homebrew/bin/hermes`
- 当前进程 `PATH` 中的 `hermes`

连接后还会校验：

- serve token 是否匹配；
- `deskpet-app` named profile 是否可用；
- Hermes `desktop_contract` 是否达到当前要求（>= 6）；
- DeskPet 专属 `~/.deskpet/hermes` profile、workspace 和 SOUL 隔离是否成立。

当前限制：

- 不是远程匹配别人的 Hermes。
- 不负责自动安装 Hermes。
- 固定候选路径和 PATH 探测尚未覆盖所有自定义 venv、包管理器和 launchd 环境。
- Hermes 未安装、版本不兼容或 profile 接入冲突时，应明确报错，禁止静默回退到默认 profile。

### 1.4 当前工作区注意事项

以下文件目前只在本地，未上传 GitHub，不要在后续提交中误加入：

- `backups/DeskPet-0.5.0.app.zip`
- `鲸鱼娘-hatch-pet-生图提示词.html`
- `鲸鱼娘-hatch-pet-生图提示词.md`

`DeskPet-0.7.0.dmg` 是本地发布产物，已上传 Release；不要把包含用户密钥的本地运行配置加入提交。

---

## 2. 下一版本四项目标（含一项产品探索）

### P0：DeskPet 启动后接入 Hermes 网关，并通过微信操作

### 用户目标

DeskPet 启动并完成 Hermes 连接后，用户可以通过微信向 Hermes 发消息、接收回复，并使用必要的任务控制能力；不要求用户打开桌面窗口才能操作。

### 目前不能直接假设的内容

“连接 Hermes 网关微信”需要先确认 Hermes 侧的真实能力，不能凭名称猜 API：

1. Hermes 当前版本是否已经提供微信 Gateway/Channel/Adapter。
2. 支持的是个人微信、企业微信、公众号、机器人 Webhook，还是其他渠道。
3. 配对方式是二维码、token、Webhook URL，还是配置文件注入。
4. 消息入口是否已经进入 Hermes 主 Agent，还是需要 DeskPet 建立转发层。
5. 回复、任务进度、任务完成、停止命令是否都有明确的事件类型。
6. Gateway 是否随 `hermes serve` 启动，还是需要独立进程/端口。
7. Hermes 升级后接口是否稳定、是否需要固定最低版本。

### 设计原则

- DeskPet 只做客户端适配，**不修改 Hermes 本体** `~/.hermes/hermes-agent/**`。
- 不把微信密钥、二维码 token、Webhook secret 写入 Git 或普通日志。
- 不把微信通道凭证复制到主 profile；优先沿用 Hermes 已有凭证机制，必要时使用 Keychain/UserDefaults 的安全存储方案。
- 桌面 GUI、微信入口必须共享同一主 Agent/任务 Agent 状态模型，不能各自维护一套任务状态。
- 微信断线、serve 重启、重复事件、迟到回复必须有可见状态，不能伪报成功。
- 远程微信用户能否操作本机文件、执行敏感任务，需要沿用现有 Hermes/DeskPet 的确认和权限边界，不能因换了入口而放宽。

### 目标链路（待 Hermes 能力核查后定稿）

```text
DeskPet 启动
  → 探测/启动并校验 Hermes serve
  → 探测/启用 Hermes Gateway
  → 微信绑定或连接
  → 微信消息
  → Gateway / Hermes 主 Agent
  → DeskPet 现有 HermesBridge 状态机
  → 主回复 / 任务结果
  → Gateway
  → 微信回复
```

### P0 验收标准

- DeskPet 启动后能明确显示：微信未配置、等待绑定、已连接、断线重连、连接失败中的一种状态。
- 微信文本可以进入正确的主会话，主 Agent 回复可以回到微信。
- 微信消息触发任务时，任务 Agent 仍使用现有单槽 FIFO、任务看门狗和归档逻辑。
- 微信可以查询任务状态；停止语义和 GUI/语音语义有明确映射。
- 断开微信或重启 serve 后不会产生重复任务、幽灵任务或错误的“已完成”提示。
- 凭证不进入源码、DMG 默认配置、普通日志和 Git 历史。
- 未完成网关能力核查前，不开始编写具体微信适配代码。

---

### P1：编写小白可用的操作说明

### 目标

让没有开发经验、但已经安装或准备安装 Hermes 的用户，能够完成：安装 DeskPet、首次启动、连接 Hermes、配置语音、绑定微信、发送第一条消息、派发任务、停止任务和排查常见问题。

### 建议交付物

1. 根目录 `README.md`：保留 GitHub 首页的极简快速开始。
2. 根目录新增面向用户的详细指南（文件名待确认，例如 `使用说明.md` 或 `USER-GUIDE.md`）。
3. 如微信绑定步骤较复杂，再在 App 内提供一条“打开操作说明/复制诊断信息”的入口；不先做完整复杂向导，避免过度建设。

### 指南目录建议

1. DeskPet 是什么，为什么必须先安装 Hermes。
2. 系统要求与 Apple Silicon/Intel 限制。
3. 安装 DeskPet 与首次启动。
4. 麦克风、语音识别和通知权限。
5. Hermes 自动匹配：成功、未找到、不兼容、多个安装的处理方式。
6. 语音输入、唤醒词、持续聆听和 TTS 配置。
7. 微信网关绑定、连接状态和首次发送消息。
8. 如何聊天、派发任务、查询状态、追加任务指令。
9. 如何停止：GUI、语音、微信三种入口的语义差异。
10. 文件工作区、会话历史、SOUL、凭证分别存在哪里。
11. 常见故障：Hermes 未安装、serve 端口占用、token/profile 冲突、微信断线、语音无声。
12. 安全边界：工作区不是系统沙箱；敏感、付费、不可逆操作仍需确认。
13. 更新、卸载和清理用户数据。

### P1 验收标准

- 按文档从全新用户视角走一遍，不依赖开发者口头补充。
- 每个失败场景都有“现象 → 原因 → 处理步骤”。
- 文档中的路径、菜单名、版本号和截图/示例与当前应用一致。
- 不在文档中放真实 API Key、token、个人微信信息或本机隐私路径。

---

### P0/P1：增强本机 Hermes 自适应匹配

### 目标边界

这里的“自适应匹配”指：**自动匹配安装在本机、属于当前用户的 Hermes**。不做远程寻找别人的 Hermes，不替用户安装 Hermes，不绕过 token/profile 校验。

### 建议探测优先级

1. 用户显式选择或保存的 Hermes 可执行文件路径。
2. 当前 DeskPet 运行环境 `PATH` 中的 `hermes`。
3. 当前用户常见 venv/包管理器路径。
4. `~/.hermes` 及其 profile 目录中的可验证安装线索。
5. 其他候选逐个做有限时长探测，不使用无限等待。

### 每个候选的验证步骤

1. 文件存在且可执行。
2. `hermes --version` 或等价版本探测成功，且有超时。
3. 能启动/连接 serve。
4. token 握手成功。
5. 请求的 profile 确实是 `deskpet-app`。
6. `desktop_contract` 达到最低版本。
7. `session.create` 能在 `~/.deskpet/hermes/workspace` 中建立会话。
8. 失败时记录候选路径和可行动原因，但不记录 token。

### 多安装处理

- 不要随机选第一个结果后静默使用。
- 如果只有一个候选通过完整验证，自动使用并在设置/日志中显示来源。
- 如果多个候选均通过，提示用户选择或提供“记住此路径”。
- 如果候选均失败，显示候选检查结果和手动修复入口。
- 旧版默认 profile、主 profile 和 DeskPet profile 的数据必须继续隔离。

### 验收矩阵

- Hermes 位于当前固定 venv 路径。
- Hermes 仅在 PATH 中可找到。
- Hermes 位于 Homebrew/自定义 venv 路径。
- Finder/launchd 启动导致 PATH 为空。
- Hermes 未安装。
- Hermes 版本过旧或不支持 desktop contract。
- 多个 Hermes 安装且只有一个可用。
- 多个 Hermes 安装均可用。
- token 错误、端口占用、profile 冲突。
- 探测失败后用户修复环境，DeskPet 可以重试，不被错误的 `ensured` 状态锁死。

---

### P0/P1：活人感与情绪价值

### 产品愿景

DeskPet 不只是“用户问一句、它答一句”的窗口，而是一个有稳定性格、会回应用户状态、能在用户需要时提供陪伴和支持的桌面助手。

这里的“活人感”重点不是监控用户做了什么，而是让互动有**温度、连续感和适度的情绪回应**：

1. 能听懂用户明确表达的疲惫、焦虑、沮丧、开心、犹豫或压力，并用合适的语气回应。
2. 能在任务开始、受阻、完成等节点提供鼓励、安慰、庆祝、提醒或安静陪伴，而不是机械报状态。
3. 能保持一致的宠物人格、称呼和口吻，记住当前对话中的有限上下文，让用户感觉是在和同一个伙伴协作。

典型场景包括：

- 用户说“今天好累”，DeskPet 先回应和陪伴，而不是立即塞一堆建议。
- 用户卡在报错、工作或学习任务上，DeskPet 先安抚情绪，再询问是否需要一起解决。
- 用户完成任务、达成目标或连续工作一段时间，DeskPet 做自然的鼓励或庆祝。
- 用户在 QQ/微信聊天时说“你来帮我接着聊天”，DeskPet 可以把情绪理解、内容起草和明确授权的实际操作结合起来。
- 用户只想安静工作时，可以让 DeskPet 降低打扰、只在关键节点反馈。

QQ/微信接续聊天只是“情绪回应 + 实际协助”的一个例子，不应成为整个计划的唯一目标。

情绪价值不等于心理诊断、假装拥有真实情感或无授权读取用户状态；第一版只根据用户主动表达、当前对话和明确授权的任务事件回应，不默认全天候监听。

### 最小可行闭环（MVP）

第一版采用**用户主动互动 + 明确表达优先**的模式：

1. 用户通过语音、文字或 GUI 与 DeskPet 交流，或明确开启陪伴/低打扰模式。
2. DeskPet 只根据用户主动说出的内容、当前对话和自身任务状态判断回应方向，不做隐蔽的情绪识别。
3. 根据情境提供安慰、鼓励、庆祝、轻提醒、实际帮助或安静陪伴；不强行给建议。
4. 用户需要解决问题时，再进入现有 Hermes 主 Agent/任务 Agent 流程；高风险动作仍先展示并等待确认。
5. 语气、称呼、播报长度和主动程度沿用 persona/voice 配置，并允许用户关闭或调整。
6. 可选的主动关怀必须默认低频、可关闭、可静音，不能持续弹窗、播报或打断。
7. 陪伴状态有明确开始、暂停、结束和停止入口；上下文过期后不继续推断用户情绪。

跨应用上下文感知、QQ/微信自动操作和连续代办属于后续能力，不能为了制造“活人感”而一开始就做成全局监听器。

### 情绪价值实现路线（待核查）

按“先有温度、再做感知”的顺序推进：

1. **用户主动表达**：从语音转写、文字和当前对话中识别用户明确表达的情绪需要；第一版最可靠、权限最少。
2. **任务生命周期事件**：利用任务开始、受阻、完成、失败和用户中断等 DeskPet 已知事件，提供自然的鼓励、安慰或收口。
3. **Persona/voice 配置**：把称呼、口吻、亲密程度、安慰方式、播报长度和主动程度做成可调整的体验配置，不让模型自行改变人格边界。
4. **用户主动提供的上下文**：用户选中文本、复制内容或说明当前场景后，DeskPet 在情绪回应之外提供总结、起草或解决问题的帮助。
5. **有限的前台上下文感知**：只有用户明确开启后，才考虑读取前台应用/窗口；不默认用 Accessibility、屏幕录制或 OCR 推断情绪。

在真实 Hermes、QQ、微信及 macOS 权限接口核查之前，不得编造自动代聊、情绪识别或跨应用操作能力。情绪回应应基于可解释信号，不把推断当成事实。

### 建议的数据与状态模型

- `EmotionContext`：用户主动表达的情绪线索、当前对话、任务状态、用户偏好的回应方式、置信度和过期时间；不保存或推断心理诊断。
- `CompanionIntent`：用户此刻需要安慰、鼓励、庆祝、轻提醒、实际帮助、安静陪伴还是停止打扰。
- `CompanionSession`：陪伴模式的开始、暂停、结束、主动程度、静音状态和作用范围；默认短生命周期。
- `CompanionPreferences`：称呼、语气、亲密程度、播报偏好和主动关怀开关；用户可随时调整。
- `ActionRequest`：情绪回应之后的实际操作请求；发送、改文件、执行命令等仍沿用现有确认和去重机制。

### 安全与用户边界

- 不把语气推断当成心理诊断，不声称真正读懂用户内心、拥有人的情感或替代专业心理帮助。
- 不利用用户脆弱、孤独或焦虑制造依赖、内疚、恐惧、消费冲动或“只有我懂你”的暗示。
- 情绪回应、主动建议和实际执行分层；能安慰不代表能读后台内容，能生成方案不代表能发消息或改文件。
- 默认不读取后台窗口、不扫描所有聊天、不保存完整聊天记录、不持续截屏、不进行隐蔽的脸部/声纹情绪识别。
- 主动关怀必须可关闭、可降频、可静音；用户说“别打扰我/停止陪伴”后立即收口。
- 任何发送、改文件、执行命令、付费、发布或不可逆动作，默认先展示计划/草稿并等待确认。
- 日志只记应用、会话状态、事件 ID 和错误，不记聊天正文、token、情绪标签或隐私截图。
- QQ/微信等对外沟通仍需明确目标会话、发送权限和必要的 AI 身份/平台合规决策。

### 情绪价值验收标准

- 用户明确表达疲惫、沮丧、压力、开心或需要陪伴时，DeskPet 能给出符合 persona 的自然回应，不机械转成任务派发。
- 用户需要解决问题时，DeskPet 能在安慰与实际帮助之间切换，并询问用户是否需要建议或行动。
- 任务开始、受阻、完成、失败和中断时，反馈有温度但不过度播报、不假装确定未知事实。
- 用户可以关闭主动关怀、调整亲密程度/语气，并能立即停止当前陪伴或播报。
- 情绪线索不会在过期后被当作当前状态，也不会跨用户或跨会话泄漏。
- QQ/微信接续聊天、网页总结、错误解释等只是具体场景，均复用同一陪伴/助手人格和现有确认边界。
- 需要执行文件、搜索或外部操作时仍走现有任务 Agent 和确认、去重机制。

---

## 3. 推荐实施顺序

### 阶段 0：接口核查与方案冻结（先做，不改代码）

- 调研当前 Hermes Gateway 文档、源码和实际启动方式。
- 确定微信接入对象和认证方式。
- 确定微信消息是否进入现有主会话，以及任务事件如何回传。
- 确定最低 Hermes 版本与需要的 desktop contract。
- 产出一页“微信通道协议草案”和“情绪陪伴与情绪价值边界草案”及决策记录。

### 阶段 1：Hermes 自适应匹配与诊断

- 抽取/扩展 Hermes discovery，不重复实现已有路径探测。
- 增加候选验证、超时、错误分类和重试。
- 增加设置页的当前 Hermes 来源、版本和 profile 状态。
- 先用离线自测覆盖候选排序、失败分类和多安装决策。

### 阶段 2：小白操作说明

- 先写文档，跟随阶段 1 的真实错误提示同步更新。
- 用全新用户路径实际走读。
- 微信协议确定后补充微信绑定章节。

### 阶段 3：活人感与情绪价值 MVP

- 先做用户主动互动下的情绪回应、陪伴、鼓励、安慰和任务节点反馈，不先做全天候情绪监控。
- 以 persona/voice 配置统一称呼、口吻、亲密程度、播报长度和主动程度，避免每个场景各自造一套人格。
- 支持“安慰/鼓励/庆祝/轻提醒/实际帮助/安静陪伴/停止打扰”等基本陪伴意图；QQ/微信接续聊天只是其中一个验收场景。
- 默认先回应和生成建议；发送消息、改文件、执行命令等外部状态改变必须先确认。
- 实现静音、降频、停止、陪伴会话过期和重复执行保护。
- 离线自测 `EmotionContext`/`CompanionIntent`/`CompanionSession` 的回应模式、过期、停止和去重逻辑。

### 阶段 4：微信 Gateway 接入

- 只接 Hermes 已有稳定入口；若没有，再单独评估是否需要 DeskPet 适配层。
- 接入连接状态、认证、消息收发、重连和关闭。
- 复用 `HermesClient`、`HermesBridge`、现有主/任务 Agent 状态，不新造第二套任务系统。
- 加入微信入口专属的身份/会话映射、情绪回应边界、授权和去重保护。
- 明确“微信远程聊天”和“本机 QQ/微信协助沟通”是否是同一通道；不能因两者都叫微信而混用权限模型。

### 阶段 5：离线验证、实机验证与 0.8 发布

- 离线自测：Hermes discovery、路径决策、错误提示、情绪陪伴状态、授权边界和文档命令检查。
- 实机验证：不同 Hermes 安装位置、serve 重启、微信断线重连、情绪陪伴/静音/停止、QQ/微信等场景的重复发送与执行保护。
- 打包前检查：最终 App/DMG 版本、架构、密钥剥离、profile/workspace/SOUL 边界、辅助功能/屏幕录制权限提示。
- 真实微信、QQ 和网络验证通过后，再创建 `v0.8.0` Release；若 Gateway/平台能力未闭合，拆为后续版本，不伪装成已支持。

---

## 4. 待用户确认的关键决策

在以下问题明确前，不要直接开始上下文助手或微信代码实现：

1. **微信类型**：个人微信扫码、企业微信机器人，还是完全按 Hermes 当前已支持的渠道来做？
2. **Gateway 归属**：优先复用 Hermes 已有 Gateway；如果 Hermes 没有现成微信通道，是否允许另行设计 DeskPet 适配层？
3. **微信权限范围**：只允许聊天和任务查询，还是允许派发/停止/文件操作等完整控制？
4. **版本拆分**：建议先做“自适应匹配 + 小白文档”，再做微信网关；也可以确认三项合并为一个 0.8 版本。
5. **指南形式**：只写 Markdown 文档，还是增加首启诊断/绑定向导？
6. **Hermes 最低版本**：是否继续以当前 `desktop_contract >= 6` 为门槛，还是随 Gateway 能力提高？
7. **情绪价值范围**：第一版只回应用户明确表达的情绪，还是允许有限的主动关怀？建议先做前者，主动关怀默认关闭。
8. **互动风格**：偏温柔陪伴、元气鼓励、克制专业，还是跟随当前 persona？建议沿用 persona，并提供强度调节。
9. **助手动作范围**：第一版先做安慰、鼓励、庆祝、轻提醒和建议，还是包含文件/命令/外部发送？建议高风险动作统一二次确认。
10. **QQ/微信定位**：作为情绪回应与实际协助的示例，还是首个重点场景？具体通道走 Hermes Gateway、Accessibility 还是手动复制降级？
11. **情绪上下文保留范围**：只保留当前对话短期状态，还是允许用户主动保存长期偏好？建议长期记忆必须显式开启。
12. **陪伴边界**：哪些内容必须转为专业帮助提示、哪些情况必须停止自动回应、是否需要向对方披露 AI 代办身份？

---

## 5. 新对话启动清单

新对话开始时：

1. 阅读 `ARCHITECTURE.md`。
2. 阅读本 `HANDOFF.md`。
3. 检查：

   ```bash
   git status --short --branch --untracked-files=all
   git log -1 --oneline --decorate
   ```

4. 首先核查 Hermes Gateway 的真实接口与当前安装版本，不要凭经验假设微信 API。
5. 重点源码入口：
   - `DeskPet/Sources/DeskPet/ServeManager.swift`
   - `DeskPet/Sources/DeskPet/HermesClient.swift`
   - `DeskPet/Sources/DeskPet/HermesBridge.swift`
   - `DeskPet/Sources/DeskPet/DeskPetHermesProfile.swift`
   - `DeskPet/Sources/DeskPet/AppDelegate.swift`
   - `DeskPet/README.md`
6. 涉及互联网调研或 GitHub/Hermes 最新接口时，先使用 `agent-reach`/`anysearch` 核实，不把过时记忆当事实。
7. 外部写入（GitHub push/release）、密钥/隐私处理、不可逆操作按项目规则先确认。
8. 除非用户明确要求，不自动运行完整测试或构建；开始发布流程时构建属于用户明确授权的动作。

---

## 6. 明确不做的事情

- 不修改 Hermes 本体代码和用户的全局 Hermes 配置来“强行适配”。
- 不把微信凭证、Hermes token、真实 API Key 写进仓库、默认配置或日志。
- 不把“自动匹配用户 Hermes”解释成远程访问其他人的 Hermes。
- 不把 `~/.deskpet/hermes/workspace` 宣称为操作系统沙箱。
- 不在微信接入中绕过已有的敏感、付费、不可逆操作确认边界。
- 不在没有真实 Gateway 协议证据时编造端点、字段或认证流程。
- 不以“活人感”为由默认推断用户情绪、制造情感依赖或进行全天候监控。
